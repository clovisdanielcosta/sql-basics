Sub teste()
Dim str As String
Dim carro As String
carro = "Fiesta"
str = _
        " SELECT m.marca, m.origem, c.modelo, c.ano " & _
        " FROM marcas as m, '" & carro & "' as c " & _
        " WHERE m.codMarca = c.codMarca " & _
        " AND m.origem IN('Italiana','Alemã','Francesa') " & _
        " ORDER BY c.ano "

MsgBox (str)

End Sub